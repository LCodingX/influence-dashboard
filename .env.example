# Supabase (public — safe for browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Supabase (private — server-side only, NEVER prefix with VITE_)
SUPABASE_SECRET_KEY=

# RunPod (private — server-side only)
RUNPOD_TEMPLATE_ID=
RUNPOD_KEY_ENCRYPTION_SECRET=
